The php-data branch contains php code that reads the contents of the data folder and dynamically uses that data in the html template output.

The node-data branch is where node.js code will be added that does the same job as the code in the php-data branch.